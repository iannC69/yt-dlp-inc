const fs = require('fs'); let code = fs.readFileSync('src/server/spotify-api.js', 'utf8'); code += \\n\nexport async function searchSpotifyAPI(query, clientId, clientSecret, accessToken = null) {\n  const market = accessToken ? 'market=from_token' : 'market=US';\n  const q = encodeURIComponent(query);\n  const res = await fetchWithRetry(\\\/v1/search?q=\\\&type=track&limit=1&\\\\\\, clientId, clientSecret, accessToken);\n  if (res && res.tracks && res.tracks.items && res.tracks.items.length > 0) {\n    return _resolveSpotifyMetadata(\\\https://open.spotify.com/track/\\\\\\, clientId, clientSecret, accessToken);\n  }\n  return null;\n}\n\; fs.writeFileSync('src/server/spotify-api.js', code); console.log('Done');

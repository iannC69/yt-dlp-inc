const fs = require('fs'); const html = fs.readFileSync('test-spotify.html', 'utf-8'); const match = html.match(/<script id="initial-state" type="text\/plain">(.*?)<\/script>/); if (match) { const data = Buffer.from(match[1], 'base64').toString('utf-8'); fs.writeFileSync('test-spotify-state.json', data); console.log('Saved state!'); } else { console.log('No initial-state found'); }

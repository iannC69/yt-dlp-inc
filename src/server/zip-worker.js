const fs = require(fs); const path = require(path); const archiver = require(archiver); const dirPath = process.argv[2]; const zipPath = process.argv[3]; const output = fs.createWriteStream(zipPath); const archive = archiver(zip, { zlib: { level: 0 } }); output.on(close, () => process.exit(0)); archive.on(error, err => { console.error(err); process.exit(1); }); archive.pipe(output); const items = fs.readdirSync(dirPath); for (const item of items) { const itemPath = path.join(dirPath, item); const stat = fs.statSync(itemPath); if (stat.isDirectory()) { archive.append(null, { name: item + \/, dosPermissions: 0x14 }); const files = fs.readdirSync(itemPath); for (const file of files) { const filePath = path.join(itemPath, file); let dosPerms = 0x20; if (file === desktop.ini || file === album.ico || file === folder.jpg || file === AlbumArtSmall.jpg) { dosPerms = 0x22; } archive.file(filePath, { name: item + \/ + file, dosPermissions: dosPerms }); } } else { archive.file(itemPath, { name: item }); } } archive.finalize();

const fs = require('fs'); const html = fs.readFileSync('test-spotify.html', 'utf-8'); const match = html.match(/<script id="initial-state" type="text\/plain">(.*?)<\/script>/); if (match) { const data = Buffer.from(match[1], 'base64').toString('utf-8'); const json = JSON.parse(data); fs.writeFileSync('test-spotify-state.json', JSON.stringify(json, null, 2)); console.log('Parsed successfully!'); } else { console.log('No initial-state found'); }
